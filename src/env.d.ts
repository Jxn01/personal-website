/// <reference types="astro/client" />

/** ISO timestamp of the current build ("mastered" date). */
declare const __BUILD_TIME__: string;
/** Short git hash of the build. */
declare const __COMMIT__: string;
